import { BankNameValidator } from './bank-name-validator';

describe('BankNameValidator', () => {
  it('should create an instance', () => {
    expect(new BankNameValidator()).toBeTruthy();
  });
});
