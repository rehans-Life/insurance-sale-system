import { ExtractControlPipe } from './extract-control.pipe';

describe('ExtractControlPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractControlPipe();
    expect(pipe).toBeTruthy();
  });
});
